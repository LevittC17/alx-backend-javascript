/**
 * Function to display a message to the standard output
 * @param message (str): The message to display
 */
const displayMessage(message) {
  console.log(message);
}

module.exports = displayMessage;
